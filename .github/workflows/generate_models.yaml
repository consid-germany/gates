name: generate_models

on:
  workflow_call:

jobs:
  generate_openapi_models:
    runs-on: ubuntu-latest  # You can choose other OS like macos-latest or windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # This action checks out your repository's code

      - name: Install dependencies
        run: |
          # Add commands to install dependencies if needed
          # For example: npm install

      - name: Grant execute permissions to script
        run: chmod +x ./api/scripts/generate_openapi_models.sh  # Grant executable permissions to the script

      - name: Generate models
        run: ./api/scripts/generate_openapi_models.sh  # Run your script
